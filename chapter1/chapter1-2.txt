テーブルの作成・・・CREATE TABLE テーブル名(列名1, データ型, 制約, 列名2, ・・・・, テーブルの制約1, ・・・);

◆命名規則

DBやテーブル、列などの名前に使ってよい文字
・半角のアルファベット
・半角の数字
・アンダーバー（_）

・名前の最初の文字は「半角のアルファベット」にすること
・名前の重複はNG


◆データ型
・CHAR型・・・文字列を入れる列に指定する。固定長文字列型
・VARCHAR型・・・文字列を入れる列に指定する。可変長文字列型

両方とも()で最大長を指定する